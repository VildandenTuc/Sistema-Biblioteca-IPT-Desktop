# ========================================
# VARIABLES DE ENTORNO - BACKEND BIBLIOTECA IPT
# ========================================
# Este archivo es un template. Copia este archivo a .env y completa con tus valores reales
# NUNCA commitees el archivo .env (está en .gitignore)

# ========================================
# BASE DE DATOS
# ========================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=dbbiblioteca
DB_USERNAME=tu_usuario_mysql
DB_PASSWORD=tu_contraseña_mysql

# ========================================
# JWT CONFIGURATION
# ========================================
# Clave secreta JWT (debe tener al menos 32 caracteres para seguridad 256 bits)
JWT_SECRET=TuClaveSecretaSuperSeguraDeAlMenos32Caracteres
# Expiración del token en milisegundos (24 horas = 86400000)
JWT_EXPIRATION=86400000

# ========================================
# BACKUP CONFIGURATION
# ========================================
# Directorio donde se almacenan los backups
# Windows: D:/backups/biblioteca
# Linux: /var/backups/biblioteca
BACKUP_DIRECTORY=D:/backups/biblioteca

# Rutas a ejecutables de MySQL (ajustar según instalación)
# Windows ejemplo: C:/Program Files/MySQL/MySQL Server 9.1/bin/mysqldump.exe
# Linux ejemplo: /usr/bin/mysqldump
MYSQLDUMP_PATH=C:/Program Files/MySQL/MySQL Server 9.1/bin/mysqldump.exe
MYSQL_PATH=C:/Program Files/MySQL/MySQL Server 9.1/bin/mysql.exe

# ========================================
# HIBERNATE/JPA (Opcional - para desarrollo)
# ========================================
# true = mostrar SQL en consola (recomendado solo en desarrollo)
# false = no mostrar SQL (recomendado en producción)
SHOW_SQL=true
FORMAT_SQL=true

# ========================================
# NOTAS IMPORTANTES:
# ========================================
# 1. Copia este archivo a .env en el mismo directorio
# 2. Completa todos los valores con tu configuración real
# 3. NUNCA commitees el archivo .env al repositorio
# 4. En producción, configura estas variables en el servidor (systemd, docker, etc.)
